set(CPACK_VERBATIM_VARIABLES YES)
set(CPACK_PACKAGE_VERSION_MAJOR ${PROJECT_VERSION_MAJOR})
set(CPACK_PACKAGE_VERSION_MINOR ${PROJECT_VERSION_MINOR})
set(CPACK_PACKAGE_VERSION_PATCH ${PROJECT_VERSION_PATCH})
set(CPACK_PACKAGE_VENDOR "Kruzhok Association")
set(CPACK_PACKAGE_CONTACT "aleksey@fedoseev.net")
set(CPACK_RESOURCE_FILE_LICENSE "${CMAKE_CURRENT_SOURCE_DIR}/LICENSE")
set(CPACK_RESOURCE_FILE_README "${CMAKE_CURRENT_SOURCE_DIR}/README.md")

# the library and the development packages
set(CPACK_DEB_COMPONENT_INSTALL YES)
set(CPACK_COMPONENTS_ALL runtime dev)
set(CPACK_DEBIAN_FILE_NAME DEB-DEFAULT)
set(CPACK_DEBIAN_PACKAGE_ARCHITECTURE "amd64")

set(CPACK_DEBIAN_PACKAGE_DESCRIPTION "The Cyberida Hierarchy Tree Geometry Library")

set(CPACK_DEBIAN_RUNTIME_PACKAGE_NAME "libhtreegeom")
set(CPACK_DEBIAN_RUNTIME_DESCRIPTION "The Cyberida Hierarchy Tree Geometry Library")

set(CPACK_DEBIAN_DEV_PACKAGE_NAME "libhtreegeom-dev")
set(CPACK_DEBIAN_DEV_DESCRIPTION "The Cyberida Hierarchy Tree Geometry Library development files: the header, the CMake finder and the documentation")
set(CPACK_DEBIAN_DEV_PACKAGE_DEPENDS "libhtreegeom (= ${CPACK_PACKAGE_VERSION})")
